export interface Book {
    id: number
    title: string
    plot: string
    author: string
    year: number
    description: string
    audience: string
    publisher: string
    pages: number
    color: string
}